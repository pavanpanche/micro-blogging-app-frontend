name: subtxt_blog
description: "pulsar: micro blogging app."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  dio: ^5.4.0
  flutter_secure_storage: ^9.0.0
  jwt_decode: ^0.3.1
  bloc: ^9.0.0
  timeago: ^3.6.0
  equatable: ^2.0.7
  flutter_bloc: ^9.1.1
  shimmer: ^3.0.0
  pull_to_refresh: ^2.0.0
  http: ^1.4.0
  intl: ^0.20.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  

  